Checking patch net/netfilter/nft_set_pipapo.c...
error: while searching for:
}

/**
 * nft_pipapo_walk() - Walk over elements
 * @ctx:	nftables API context
 * @set:	nftables API set representation
 * @iter:	Iterator
 *
 * As elements are referenced in the mapping array for the last field, directly
 * scan that array: there's no need to follow rule mappings from the first
 * field.
 */
static void nft_pipapo_walk(const struct nft_ctx *ctx, struct nft_set *set,
			    struct nft_set_iter *iter)
{
	struct nft_pipapo *priv = nft_set_priv(set);
	const struct nft_pipapo_match *m;
	const struct nft_pipapo_field *f;
	unsigned int i, r;

	WARN_ON_ONCE(iter->type != NFT_ITER_READ &&
		     iter->type != NFT_ITER_UPDATE);

	rcu_read_lock();
	if (iter->type == NFT_ITER_READ)
		m = rcu_dereference(priv->match);
	else
		m = priv->clone;

	if (unlikely(!m))
		goto out;

	for (i = 0, f = m->f; i < m->field_count - 1; i++, f++)
		;


error: patch failed: net/netfilter/nft_set_pipapo.c:2106
error: while searching for:

		iter->err = iter->fn(ctx, set, iter, &e->priv);
		if (iter->err < 0)
			goto out;

cont:
		iter->count++;
	}

out:
	rcu_read_unlock();
}

/**

error: patch failed: net/netfilter/nft_set_pipapo.c:2151
Applying patch net/netfilter/nft_set_pipapo.c with 2 rejects...
Rejected hunk #1.
Rejected hunk #2.
