From a46440defe90793ed625b720770f52783b2ce67d Mon Sep 17 00:00:00 2001
From: Dong Chenchen <superdcc97@163.com>
Date: Tue, 21 Apr 2026 19:22:35 +0800
Subject: [PATCH 6/6] percpu_counter: shard percpu_counters list and lock per
 NUMA node

On large NUMA systems (e.g., 320 cores, 4 NUMA nodes), the global
percpu_counters_lock becomes a significant bottleneck during high
concurrency fork/exit workloads. The percpu_counters list is global,
meaning all fork/exit operations (list add/del) contend on a single
spinlock, while CPU hotplug traversal holds the same lock.

Apply sharded locks by making the percpu_counters list and its lock
per-NUMA-node. Each NUMA node maintains its own list_head and spinlock
in the percpu_counters_numa[] array.

- Init paths (percpu_counter_init*) use numa_node_id() to determine
  the current NUMA node and add counters to the per-node list
- Destroy/detach paths use the stored numa_node field to find the
  correct per-node lock
- CPU hotplug (percpu_counter_cpu_dead) iterates all NUMA nodes,
  acquiring each per-node lock sequentially

This ensures fork/exit operations on different NUMA nodes do not
contend with each other, and CPU hotplug only blocks fork/exit
operations on one NUMA node at a time.

The percpu_counters_numa[] array is initialized by
percpu_counter_init_early() called from start_kernel(), which runs
on the boot CPU before SMP bringup and before the first
percpu_counter_init_many() call, eliminating any concurrency
concerns without requiring additional locking for initialization.

When CONFIG_NUMA is not set, MAX_NUMNODES=1 and the code degrades
to the original single global list+lock behavior.

Signed-off-by: Dong Chenchen <superdcc97@163.com>
---
 include/linux/percpu_counter.h |   7 +++
 init/main.c                    |   2 +
 lib/percpu_counter.c           | 111 +++++++++++++++++++++++----------
 3 files changed, 87 insertions(+), 33 deletions(-)

diff --git a/include/linux/percpu_counter.h b/include/linux/percpu_counter.h
index 9362d88494a2..036870ee4085 100644
--- a/include/linux/percpu_counter.h
+++ b/include/linux/percpu_counter.h
@@ -24,12 +24,19 @@ struct percpu_counter {
 	s64 count;
 #ifdef CONFIG_HOTPLUG_CPU
 	struct list_head list;	/* All percpu_counters are on a list */
+	int numa_node;		/* NUMA node this counter belongs to */
 #endif
 	s32 __percpu *counters;
 };
 
 extern int percpu_counter_batch;
 
+#ifdef CONFIG_HOTPLUG_CPU
+void percpu_counter_init_early(void);
+#else
+static inline void percpu_counter_init_early(void) { }
+#endif
+
 int __percpu_counter_init_many(struct percpu_counter *fbc, s64 amount,
 			       gfp_t gfp, u32 nr_counters,
 			       struct lock_class_key *key);
diff --git a/init/main.c b/init/main.c
index 1cb395dd94e4..fd1770d504b1 100644
--- a/init/main.c
+++ b/init/main.c
@@ -33,6 +33,7 @@
 #include <linux/console.h>
 #include <linux/nmi.h>
 #include <linux/percpu.h>
+#include <linux/percpu_counter.h>
 #include <linux/kmod.h>
 #include <linux/kprobes.h>
 #include <linux/kmsan.h>
@@ -1069,6 +1070,7 @@ void start_kernel(void)
 	trap_init();
 	mm_core_init();
 	maple_tree_init();
+	percpu_counter_init_early();
 	poking_init();
 	ftrace_init();
 
diff --git a/lib/percpu_counter.c b/lib/percpu_counter.c
index ec20be17c66f..e6bbe86f7111 100644
--- a/lib/percpu_counter.c
+++ b/lib/percpu_counter.c
@@ -9,10 +9,26 @@
 #include <linux/cpu.h>
 #include <linux/module.h>
 #include <linux/debugobjects.h>
+#include <linux/topology.h>
+#include <linux/nodemask.h>
 
 #ifdef CONFIG_HOTPLUG_CPU
-static LIST_HEAD(percpu_counters);
-static DEFINE_SPINLOCK(percpu_counters_lock);
+struct percpu_counter_numa {
+	struct list_head list;
+	spinlock_t lock;
+};
+
+static struct percpu_counter_numa percpu_counters_numa[MAX_NUMNODES];
+
+void __init percpu_counter_init_early(void)
+{
+	int nid;
+
+	for (nid = 0; nid < MAX_NUMNODES; nid++) {
+		INIT_LIST_HEAD(&percpu_counters_numa[nid].list);
+		spin_lock_init(&percpu_counters_numa[nid].lock);
+	}
+}
 #endif
 
 #ifdef CONFIG_DEBUG_OBJECTS_PERCPU_COUNTER
@@ -215,10 +231,16 @@ int __percpu_counter_init_many(struct percpu_counter *fbc, s64 amount,
 	}
 
 #ifdef CONFIG_HOTPLUG_CPU
-	spin_lock_irqsave(&percpu_counters_lock, flags);
-	for (i = 0; i < nr_counters; i++)
-		list_add(&fbc[i].list, &percpu_counters);
-	spin_unlock_irqrestore(&percpu_counters_lock, flags);
+	{
+		int nid = numa_node_id();
+
+		spin_lock_irqsave(&percpu_counters_numa[nid].lock, flags);
+		for (i = 0; i < nr_counters; i++) {
+			fbc[i].numa_node = nid;
+			list_add(&fbc[i].list, &percpu_counters_numa[nid].list);
+		}
+		spin_unlock_irqrestore(&percpu_counters_numa[nid].lock, flags);
+	}
 #endif
 	return 0;
 }
@@ -264,10 +286,16 @@ int percpu_counter_init_many_pcpu(struct percpu_counter *fbc, s64 amount,
 	}
 
 #ifdef CONFIG_HOTPLUG_CPU
-	spin_lock_irqsave(&percpu_counters_lock, flags);
-	for (i = 0; i < nr_counters; i++)
-		list_add(&fbc[i].list, &percpu_counters);
-	spin_unlock_irqrestore(&percpu_counters_lock, flags);
+	{
+		int nid = numa_node_id();
+
+		spin_lock_irqsave(&percpu_counters_numa[nid].lock, flags);
+		for (i = 0; i < nr_counters; i++) {
+			fbc[i].numa_node = nid;
+			list_add(&fbc[i].list, &percpu_counters_numa[nid].list);
+		}
+		spin_unlock_irqrestore(&percpu_counters_numa[nid].lock, flags);
+	}
 #endif
 	return 0;
 }
@@ -315,10 +343,16 @@ int percpu_counter_reinit_many_pcpu(struct percpu_counter *fbc, s64 amount,
 	}
 
 #ifdef CONFIG_HOTPLUG_CPU
-	spin_lock_irqsave(&percpu_counters_lock, flags);
-	for (i = 0; i < nr_counters; i++)
-		list_add(&fbc[i].list, &percpu_counters);
-	spin_unlock_irqrestore(&percpu_counters_lock, flags);
+	{
+		int nid = numa_node_id();
+
+		spin_lock_irqsave(&percpu_counters_numa[nid].lock, flags);
+		for (i = 0; i < nr_counters; i++) {
+			fbc[i].numa_node = nid;
+			list_add(&fbc[i].list, &percpu_counters_numa[nid].list);
+		}
+		spin_unlock_irqrestore(&percpu_counters_numa[nid].lock, flags);
+	}
 #endif
 
 	return 0;
@@ -353,10 +387,14 @@ void percpu_counter_detach_many(struct percpu_counter *fbc, u32 nr_counters)
 		debug_percpu_counter_deactivate(&fbc[i]);
 
 #ifdef CONFIG_HOTPLUG_CPU
-	spin_lock_irqsave(&percpu_counters_lock, flags);
-	for (i = 0; i < nr_counters; i++)
-		list_del(&fbc[i].list);
-	spin_unlock_irqrestore(&percpu_counters_lock, flags);
+	{
+		int nid = fbc[0].numa_node;
+
+		spin_lock_irqsave(&percpu_counters_numa[nid].lock, flags);
+		for (i = 0; i < nr_counters; i++)
+			list_del(&fbc[i].list);
+		spin_unlock_irqrestore(&percpu_counters_numa[nid].lock, flags);
+	}
 #endif
 
 	for (i = 0; i < nr_counters; i++)
@@ -379,10 +417,14 @@ void percpu_counter_destroy_many(struct percpu_counter *fbc, u32 nr_counters)
 		debug_percpu_counter_deactivate(&fbc[i]);
 
 #ifdef CONFIG_HOTPLUG_CPU
-	spin_lock_irqsave(&percpu_counters_lock, flags);
-	for (i = 0; i < nr_counters; i++)
-		list_del(&fbc[i].list);
-	spin_unlock_irqrestore(&percpu_counters_lock, flags);
+	{
+		int nid = fbc[0].numa_node;
+
+		spin_lock_irqsave(&percpu_counters_numa[nid].lock, flags);
+		for (i = 0; i < nr_counters; i++)
+			list_del(&fbc[i].list);
+		spin_unlock_irqrestore(&percpu_counters_numa[nid].lock, flags);
+	}
 #endif
 
 	free_percpu(fbc[0].counters);
@@ -407,23 +449,26 @@ static int percpu_counter_cpu_dead(unsigned int cpu)
 {
 #ifdef CONFIG_HOTPLUG_CPU
 	struct percpu_counter *fbc;
+	int nid;
 
 	compute_batch_value(cpu);
 
-	spin_lock_irq(&percpu_counters_lock);
-	list_for_each_entry(fbc, &percpu_counters, list) {
-		s32 *pcount;
+	for (nid = 0; nid < nr_node_ids; nid++) {
+		spin_lock_irq(&percpu_counters_numa[nid].lock);
+		list_for_each_entry(fbc, &percpu_counters_numa[nid].list, list) {
+			s32 *pcount;
 
-		if (!fbc->counters)
-			continue;
+			if (!fbc->counters)
+				continue;
 
-		raw_spin_lock(&fbc->lock);
-		pcount = per_cpu_ptr(fbc->counters, cpu);
-		fbc->count += *pcount;
-		*pcount = 0;
-		raw_spin_unlock(&fbc->lock);
+			raw_spin_lock(&fbc->lock);
+			pcount = per_cpu_ptr(fbc->counters, cpu);
+			fbc->count += *pcount;
+			*pcount = 0;
+			raw_spin_unlock(&fbc->lock);
+		}
+		spin_unlock_irq(&percpu_counters_numa[nid].lock);
 	}
-	spin_unlock_irq(&percpu_counters_lock);
 #endif
 	return 0;
 }
-- 
2.43.0

